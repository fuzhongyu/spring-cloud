spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fuzhongyu/spring-cloud-config-repo
#          search-paths: repo
#          username:
#          password:

server:
  port: 8082