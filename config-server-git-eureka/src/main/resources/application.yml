spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fuzhongyu/spring-cloud-config-repo


server:
  port: 8082

#配置注册服务器
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/

